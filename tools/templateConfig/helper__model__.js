
export const get__model__(pascalCase) = async () => {
    // const data = await fetch(`http://localhost:3000/user`);
    // return  data.json();
}

export const add__model__(pascalCase) = async (addData) => {
    // if(!addData) return Promise.reject('No data to add');
    // const data = await fetch(`http://localhost:3000/user`, {
    //     method: 'POST',
    //     headers: {'Content-Type': 'application/json'},
    //     body : JSON.stringify(addData)
    //
    // });
    //
    // return data.json();
}

export const delete__model__(pascalCase) = async (id) => {
    // if(!id) return Promise.reject('No id to delete');
    // const data = await fetch(`http://localhost:3000/user/${id}`, {
    //     method: 'DELETE',
    //
    // });
    //
    // return data;
}

export const update__model__(pascalCase) = async (id,updateData) => {
    // if(!id) return Promise.reject('No id to update');
    // if(!updateData) return Promise.reject('No data to update');
    // const data = await fetch(`http://localhost:3000/user/${id}`, {
    //     method: 'PUT',
    //     headers: {'Content-Type': 'application/json'},
    //     body : JSON.stringify(updateData)
    //
    //
    // });
    // return  data.json();

}